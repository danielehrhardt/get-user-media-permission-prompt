import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'getUserMedia Permission Prompt',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
